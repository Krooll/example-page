import styles from './AccessoriesList.module.scss';

const AccessoriesList = () => {
    return(
        <div>
            <h1>Accesories</h1>
        </div>
    );
};

export default AccessoriesList;